_site/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/
.DS_Store

# Plaintext travels, from back when the page was gated
travels.source.json

# The camera-roll originals behind the year galleries: ~196 MB, and the site
# only ships what tools/photos.py makes out of them. See _originals/README.md.
_originals/
